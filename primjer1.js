/*
class RandomGenerator {
  constructor(qoutes) {
    this.qoutes = qoutes;
  }

  newQuote() {
    let qoutes = this.qoutes;

    if (qoutes.length > 0) {
      let randNumber = Math.floor(Math.random() * qoutes.length);
      return qoutes[randNumber];
    } else {
      return "Nema recenica u listi";
    }
  }
}

module.exports = RandomGenerator;
*/
